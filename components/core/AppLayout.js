import { Layout } from "antd";

const { Header, Content, Footer } = Layout;

const AppLayout = () => {
  return (
    <Layout>
      <Header></Header>
    </Layout>
  );
};

export default AppLayout;
